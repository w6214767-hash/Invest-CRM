name: InvestScan requires the standalone backend

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  instructions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "InvestScan uses its own API and database. Deploy with deploy/investscan/compose.yml; static GitHub Pages is not supported."
